import React from 'react';

import { Container, Title } from './styles';

const Detail: React.FC = () => {
  return (
    <Container>
      <Title>Detail</Title>
    </Container>
  );
};

export default Detail;
